on:
  pull_request:
  push:
    branches: [main]

jobs:
  build:
    services:
      postgres:
        image: postgres
        env:
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    name: Rust build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install stable rust
        run: rustup toolchain install stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: "backend"
      - name: Build
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost/appcenter_website
        run: |
          cd backend
          cargo build --workspace --all-features
          cargo test --workspace